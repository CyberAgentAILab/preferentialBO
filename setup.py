from setuptools import setup, find_packages

setup(
    name="preferentialBO",
    version="0.1",
    description="A package for preferential Bayesian optimization",
    packages=find_packages(),
)
